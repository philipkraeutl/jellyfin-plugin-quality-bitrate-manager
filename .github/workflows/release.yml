name: Release plugin

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Four-part plugin version, for example 1.0.0.1"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  release:
    name: Build and release all Jellyfin variants
    runs-on: ubuntu-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET 9 and 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            9.0.x
            10.0.x

      - name: Determine and validate release version
        id: version
        shell: pwsh
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          $version = if ($env:GITHUB_REF_TYPE -eq 'tag') {
              $env:GITHUB_REF_NAME -replace '^v', ''
          } else {
              $env:DISPATCH_VERSION.Trim() -replace '^v', ''
          }
          if ($version -notmatch '^\d+\.\d+\.\d+\.\d+$') {
              throw "Release version '$version' must contain four numeric parts."
          }
          $manifestVersion = (Select-String -Path build.yaml -Pattern '^version:\s*"?([^"\s]+)"?\s*$').Matches.Groups[1].Value
          [xml]$project = Get-Content src/Jellyfin.Plugin.QualityBitrateManager/Jellyfin.Plugin.QualityBitrateManager.csproj
          $projectVersion = $project.SelectSingleNode('//PluginVersion').InnerText
          if ($version -ne $manifestVersion -or $version -ne $projectVersion) {
              throw "Version mismatch: release=$version, build.yaml=$manifestVersion, PluginVersion=$projectVersion"
          }
          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=v$version" >> $env:GITHUB_OUTPUT

      - name: Test Jellyfin 10.11 build
        run: |
          dotnet restore QualityBitrateManager.slnx -p:JellyfinTargetFramework=net9.0 -p:JellyfinVersion=10.11.0
          dotnet test QualityBitrateManager.slnx -c Release --no-restore -p:JellyfinTargetFramework=net9.0 -p:JellyfinVersion=10.11.0

      - name: Test Jellyfin 12 build
        run: |
          dotnet restore QualityBitrateManager.slnx -p:JellyfinTargetFramework=net10.0 -p:JellyfinVersion=12.0.0-rc3
          dotnet test QualityBitrateManager.slnx -c Release --no-restore -p:JellyfinTargetFramework=net10.0 -p:JellyfinVersion=12.0.0-rc3

      - name: Package both variants
        shell: pwsh
        run: |
          ./scripts/package.ps1 -Version "${{ steps.version.outputs.version }}" -Framework net9.0 -JellyfinVersion 10.11.0
          ./scripts/package.ps1 -Version "${{ steps.version.outputs.version }}" -Framework net10.0 -JellyfinVersion 12.0.0-rc3 -PackageSuffix "-jellyfin12"

      - name: Update both manifest entries
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          $manifest = @(Get-Content manifest.json -Raw | ConvertFrom-Json)
          $timestamp = (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
          $variants = @(
            [ordered]@{
              version = $env:RELEASE_VERSION
              changelog = "Release for Jellyfin 10.11.x. See the GitHub release notes."
              targetAbi = "10.11.0.0"
              file = "quality-bitrate-manager_$env:RELEASE_VERSION.zip"
            },
            [ordered]@{
              version = $env:RELEASE_VERSION
              changelog = "Build for Jellyfin 12.0. See the GitHub release notes."
              targetAbi = "12.0"
              file = "quality-bitrate-manager-jellyfin12_$env:RELEASE_VERSION.zip"
            }
          )
          $entries = foreach ($variant in $variants) {
            $archive = "artifacts/$($variant.file)"
            [ordered]@{
              version = $variant.version
              changelog = $variant.changelog
              targetAbi = $variant.targetAbi
              sourceUrl = "https://github.com/$env:GITHUB_REPOSITORY/releases/download/$env:RELEASE_TAG/$($variant.file)"
              checksum = (Get-FileHash -Algorithm MD5 $archive).Hash.ToUpperInvariant()
              timestamp = $timestamp
            }
          }
          $abis = @("10.11.0.0", "12.0")
          $olderVersions = @($manifest[0].versions | Where-Object { $_.version -ne $env:RELEASE_VERSION -or $_.targetAbi -notin $abis })
          $manifest[0].versions = @($entries) + $olderVersions
          ConvertTo-Json -InputObject $manifest -Depth 10 | Set-Content manifest.json -Encoding utf8

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: quality-bitrate-manager-${{ steps.version.outputs.version }}
          path: |
            artifacts/quality-bitrate-manager_${{ steps.version.outputs.version }}.zip
            artifacts/quality-bitrate-manager_${{ steps.version.outputs.version }}.zip.sha256
            artifacts/quality-bitrate-manager-jellyfin12_${{ steps.version.outputs.version }}.zip
            artifacts/quality-bitrate-manager-jellyfin12_${{ steps.version.outputs.version }}.zip.sha256
            manifest.json
          if-no-files-found: error

      - name: Create GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          gh release create "$RELEASE_TAG" \
            "artifacts/quality-bitrate-manager_${RELEASE_VERSION}.zip" \
            "artifacts/quality-bitrate-manager_${RELEASE_VERSION}.zip.sha256" \
            "artifacts/quality-bitrate-manager-jellyfin12_${RELEASE_VERSION}.zip" \
            "artifacts/quality-bitrate-manager-jellyfin12_${RELEASE_VERSION}.zip.sha256" \
            --target "$GITHUB_SHA" \
            --title "Quality Bitrate Manager $RELEASE_VERSION" \
            --generate-notes

      - name: Publish updated manifest
        shell: bash
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          cp manifest.json "$RUNNER_TEMP/manifest.json"
          git fetch origin "$DEFAULT_BRANCH"
          git checkout -B "$DEFAULT_BRANCH" "origin/$DEFAULT_BRANCH"
          cp "$RUNNER_TEMP/manifest.json" manifest.json
          if ! git diff --quiet -- manifest.json; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add manifest.json
            git commit -m "Update plugin manifest for v${RELEASE_VERSION}"
            git push origin "HEAD:${DEFAULT_BRANCH}"
          fi
